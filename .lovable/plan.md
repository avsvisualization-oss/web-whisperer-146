

## Plan: Remove InteractiveSection from Homepage

Remove the `<InteractiveSection />` component from `src/pages/Index.tsx` and its unused import. Everything else stays exactly as-is.

### Changes

**File: `src/pages/Index.tsx`**
- Remove `import InteractiveSection` (line 4)
- Remove `<InteractiveSection />` from the JSX (line 23)

